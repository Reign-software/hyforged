# Generic Registry Utilities

Package: `com.hypixel.hytale.registry`

- `Registry` — a general purpose registry container.
- `Registration` — represents a registered entry in a registry.

This package is used as a common pattern across multiple subsystems.
