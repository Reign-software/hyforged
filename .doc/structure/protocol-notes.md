# Protocol Notes (Supplement)

`PacketRegistry` is very large and enumerates all packet types. For quick navigation:
- Search by folder under `protocol/packets/` (asseteditor, auth, inventory, world, etc.).
- Use `ProtocolSettings` for protocol version, counts, and hash.

This file exists as a placeholder for future packet‑level indexing.
